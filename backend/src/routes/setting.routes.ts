import { Router } from 'express';

const router = Router();

// TODO: GET / - 获取所有配置
// TODO: GET /:category - 按类别获取配置
// TODO: PUT /:category/:key - 更新单个配置
// TODO: POST /batch - 批量更新配置
// TODO: POST /test-ai - 测试 AI 连接
// TODO: POST /test-smtp - 测试邮件连接

export default router;
