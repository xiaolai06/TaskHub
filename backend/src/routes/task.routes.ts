import { Router } from 'express';

const router = Router();

// TODO: GET / - 任务列表（支持按项目/状态/负责人筛选）
// TODO: POST / - 创建任务
// TODO: GET /:id - 任务详情
// TODO: PUT /:id - 更新任务
// TODO: DELETE /:id - 删除任务
// TODO: PATCH /:id/status - 更新任务状态
// TODO: GET /project/:projectId - 获取项目下所有任务

export default router;
