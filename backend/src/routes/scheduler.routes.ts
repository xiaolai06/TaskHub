import { Router } from 'express';

const router = Router();

// TODO: GET /project/:projectId - 项目排期视图
// TODO: POST /suggest - AI 排期建议
// TODO: POST /reschedule - 重新排期（延期/冲突处理）
// TODO: GET /conflicts - 检测排期冲突

export default router;
