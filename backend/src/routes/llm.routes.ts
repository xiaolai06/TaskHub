import { Router } from 'express';

const router = Router();

// TODO: POST /chat - AI 对话（SSE 流式）
// TODO: GET /conversations - 获取对话历史
// TODO: GET /conversations/:sessionId - 获取会话详情
// TODO: DELETE /conversations/:sessionId - 删除会话
// TODO: POST /parse-task - AI 解析自然语言任务

export default router;
