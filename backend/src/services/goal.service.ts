import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();

// TODO: findAll(userId, filters) - 目标列表
// TODO: findById(id) - 目标详情
// TODO: create(data) - 创建目标
// TODO: update(id, data) - 更新目标
// TODO: delete(id) - 删除目标
// TODO: updateProgress(id, value) - 更新进度
