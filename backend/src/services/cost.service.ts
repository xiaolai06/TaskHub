import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();

// TODO: findAll(filters) - 成本列表
// TODO: findById(id) - 成本详情
// TODO: create(data) - 创建成本记录
// TODO: update(id, data) - 更新成本
// TODO: delete(id) - 删除成本
// TODO: getSummaryByProject(projectId) - 按类别汇总项目成本
